"""Named AI Assistant workflow compositions (Tier 2, commercial).

Each workflow is a deterministic orchestration over verified PrismHR
reads, producing a structured finding object. MCP tool wrappers live
alongside each module and are registered only by the commercial server.
"""
